# 3M Instrument Logger — 전체 개발 이슈 및 성과 정리

> 프로젝트 시작부터 현재까지 발생한 버그·실패 사례와 성공적으로 완료된 기능을 구분해 기록한 문서.  
> 최종 업데이트: 2026-07-08

---

## ❌ 문제가 되었던 부분 (버그 / 실패 / 오기재)

### 1. EXE 런처 카드 미표시 — 3중 원인 (2026-06-23)

가장 까다로웠던 버그. 내비게이션 바와 "계측기를 선택하세요" 텍스트는 정적 HTML이라
JavaScript가 실행되지 않아도 표시되어 — 진단이 극히 어려웠음.

| 원인 | 내용 | 해결 |
|---|---|---|
| **dist 스탤** | `dist/sp2100_logger.js`에 `const isNum` 중복 선언 → ES 모듈 SyntaxError → import 체인 조용히 전체 실패 | dist에서 중복 선언 1줄 제거 |
| **MIME 타입** | PyInstaller 환경에서 `.js` → `application/octet-stream` 반환 → Chromium이 ES 모듈 거부 | `package_exe.py`의 `Handler.guess_type()` 메서드 오버라이드 |
| **localStorage 크기** | 사진 base64 포함 JSON ~41KB를 HTML `<head>` 인라인 삽입 → Qt WebEngine 파싱 실패 | 동기 XHR 방식(`/api/storage/data`)으로 변경 |

> ⚠️ **핵심 함정**: ES 모듈 import 오류는 try-catch로 잡히지 않고 조용히 실패함.

---

### 2. DAQ-6510 SCPI 에러 연속 발생

| 에러 | 원인 | 해결 |
|---|---|---|
| `-113 Undefined header` | `ROUT:TERM REAR`는 read-only 명령 — 쓰기 전송 불가 | 연결 시 해당 명령 제거 |
| `-113` (TRIG:COUN) | `ROUT:SCAN:CRE` 사용 시 트리거 자동 구성 — `TRIG:COUN` 전송 시 충돌 | `TRIG:COUN` 사용 금지 |
| `Error 2859` | `READ?`는 단일 채널만 반환 — 버퍼에 1개뿐인데 `TRAC:DATA? 1,10`으로 10개 요청 | `ROUT:SCAN:CRE` 폐기, 수동 릴레이 제어 채택 |
| `Error 1135` | `TRAC:DATA?` + `INIT` 조합 사용 시 발생 | 해당 방식 완전 폐기 |
| 채널 딜레이 명령 오류 | `ROUT:SCAN:DELay` (잘못된 키워드) | `ROUT:CHAN:DELay`로 수정 |

---

### 3. Agilent 4339B -213 에러 (INIT IGNORED)

- **원인**: `TRIG:SOUR BUS` 없이 `:MEAS:RES?` 사용 — BUS 트리거 모드와 충돌
- **잘못 추가된 명령**: `:SENS:MODE SURF/VOL`, `:SENS:CURR:RANG:UPP` — Python 미사용 명령 임의 추가
- **해결**: Python 단독 프로그램 기준으로 시퀀스 전면 재작성

---

### 4. SP-2100 파형 디코딩 불가 — 종결 ⛔

- 장비 측에서 파형 데이터 수집 자체가 불가한 것으로 최종 확인
- 바이너리 디코딩 포맷(bit-depth, SCALE, OFFSET) 미확인 상태에서 임의 포맷 적용 시 노이즈처럼 표시됨
- **결정**: `sp2100_logger.js`의 `decodeWave` 코드 현 상태 동결, 파형 그래프 미구현

---

### 5. SP-2100 / TL-2200 보드레이트 오기재

- LinkLoop 노트 2곳에 `9600 bps`로 잘못 기재
- 실제: SP-2100 = **57600 bps**, TL-2200 = **38400 bps**
- 수정 완료 (2026-06-24)

---

### 6. BoxPlot 클래스 삭제 → 앱 전체 빈 페이지

- `core.js` 대량 수정 중 `BoxPlot` 클래스 정의 통째로 삭제됨
- `ReferenceError: BoxPlot is not defined` → try-catch로도 잡히지 않음
- **교훈**: `core.js` 수정 시 핵심 클래스(`BoxPlot`, `EditableGrid`, `StateMachine`) 훼손 여부를 Diff로 반드시 확인

---

### 7. 그리드 셀 캐시 누적 버그

- `EditableGrid._render()` 재호출 시 `this._cells` 리스트 미초기화 → 누적
- detached Input 요소에 데이터 기입 → 화면에 반영 안 됨
- **해결**: `_render()` 시작 시 `this._cells = []` 추가

---

### 8. 그리드 레이아웃 Right Panel 찌그러짐

- 테이블 가로 폭 증가 시 Center 컬럼이 줄어들지 않아 우측 차트 패널이 화면 밖으로 밀림
- **해결**: `grid-template-columns`에 `minmax(0, 1fr)`, `#center`에 `min-width: 0`

---

### 9. 차트 캔버스 찌그러짐 2건

| 위치 | 원인 | 해결 |
|---|---|---|
| Agilent `#agChart` | `index.css` 셀렉터 누락 | `index.css`에 `#agChart` 추가 |
| SP-2100 `.graph-area` | `min-height` 없음 | `.graph-area { min-height: 240px }` 추가 |

---

### 10. AI Photo Editor 카메라 연동 버그 4건

| 버그 | 원인 | 해결 |
|---|---|---|
| 초점 링 위치 오류 | CSS `zoom` 적용 시 `offsetX/Y` 좌표 불일치 | 부모 요소 traversal로 절대 좌표 계산 |
| 이미지 크기 캐시 오염 | `img.naturalWidth` 매번 읽어 이전 이미지 크기 섞임 | `_liveNatW/_liveNatH` 캐시 변수로 분리 |
| 재시도 속도 느림 | `onerror` 재시도 간격 400ms | 100ms로 단축 |
| 카메라 창 잔상 | `openPhone()` 호출 시 기존 span 중복 생성 | 기존 span 먼저 제거 후 재생성 |

---

### 11. EXE 빌드 오류 4종

| 오류 | 원인 | 해결 |
|---|---|---|
| `PermissionError WinError 5` | EXE 실행 중 재빌드 — 파일 잠김 | `Stop-Process` + `Remove-Item` |
| ClubExpense 누락 | `build_standalone.py` 인수 누락 | 전체 계측기 목록으로 재실행 |
| `PermissionError Errno 13` | dist 하위 폴더 읽기전용 파일 잔존 | `attrib -r -s dist\*.* /s /d` 후 삭제 |
| 긴 폴더명 | 계측기명이 `_`로 연결된 폴더명 생성 | `Rename-Item`으로 `3M_Instrument_Logger`로 단축 |

---

### 12. Mitutoyo VL-50 그룹 개수 입력 혼란

- 그리드 뷰에 수동 "그룹 개수" 입력 필드가 테스트 방법 설정과 충돌
- **해결**: 수동 입력 필드 제거, 테스트 방법 속성값(`cols`, `group`, `set`)으로 자동 연동

---

### 13. Photo Editor UI 라벨 오류 (2026-06-25)

- 상단 툴바의 "한 열" 텍스트가 기능과 맞지 않는 표현
- **해결**: "한 열" → "그룹 개수"로 수정 (`ai_photo_editor.js` 704번 라인)

---

### 14. PST-3202 CSS 클래스 불일치 (2026-07-03)

| 불일치 | JS 코드 | CSS에 잘못 정의된 선택자 | 해결 |
|---|---|---|---|
| 채널 카드 선택 상태 | `pst-sel` 클래스 추가 | `.pst-ch-card.selected` | `.pst-ch-card.pst-sel`로 수정 (4곳) |
| 전압 LED 색상 | `pst-v` 클래스 | `.pst-led-val.v` | `.pst-led-val.pst-v`로 수정 |
| 전류 LED 색상 | `pst-i` 클래스 | `.pst-led-val.i` | `.pst-led-val.pst-i`로 수정 |

---

### 15. PST-3202 `initTabLock()` 호출 타이밍 오류 (2026-07-03)

- `initTabLock()`이 `init()` 내부에서 호출됨 → `buildCenter()` 실행 전이라 DOM에 모달 미존재
- 결과: 탭락 모달 기능 전혀 동작 안 함 (querySelector 실패, 오류 없이 silently 무시)
- **해결**: `buildCenter()` 끝에서 `el.innerHTML` 설정 완료 후 `initTabLock()` 호출

---

### 16. PST-3202 OVP 버튼 무응답 (2026-07-03) ✅ 해결 완료

- `setOVP()` 내부에서 브라우저 `prompt()` 사용
- pywebview 환경 및 일부 Chrome 보안 정책에서 `prompt()` 완전 차단 → 클릭해도 반응 없음
- OVP는 장비 연결 후에만 사용 가능 (정상 동작 조건)
- **해결**: `pstOvpModal` 커스텀 모달로 교체 (`_ovpCh` 상태, `applyOvp()`, `closeOvpModal()` 추가)

---

### 17. 테마 감지 방식 오류 — `dataset.theme` 미사용 (2026-07-03)

> ⚠️ **PST-3202 구현 중 발견된 함정 — 향후 모든 계측기 모듈에 해당**

- `core.js`의 테마 적용 코드: `document.body.classList.toggle('light', theme === 'light')`
- 잘못된 감지: `document.documentElement.dataset.theme === 'light'` → **항상 false** (해당 속성 자체가 없음)
- 결과: `drawGraph()` 배경색이 라이트 테마에서도 항상 `#060e17`(검정)으로 고정
- CSS도 마찬가지: `:root[data-theme="light"]` 선택자 → 절대 매칭 안 됨
- **올바른 감지 (JS)**: `document.body.classList.contains('light')`
- **올바른 선택자 (CSS)**: `body.light .대상요소`

---

### 18. EXE 파일 저장 시 데이터 손상 — base64 청크 인코딩 버그 (2026-07-04) ✅ 해결 완료

> 전체 코드 점검 중 발견. **8KB를 초과하는 모든 EXE 내보내기 파일이 조용히 손상**되던 심각한 버그.

- `standalone.html`의 EXE 저장 훅이 blob을 8192바이트 청크로 나눠 **청크마다 `btoa()`** 호출 후 이어붙임
- 8192는 3의 배수가 아니라서 각 청크의 base64 끝에 패딩(`=`)이 생김 → 이어붙인 문자열은 유효하지 않은 base64
- **함정**: Python 서버의 `base64.b64decode`가 **오류 없이** 디코딩하지만 결과가 손상됨
  (재현 테스트: 25,600바이트 → 25,601바이트, 내용 불일치) — 오류가 안 나서 발견이 어려웠음
- **해결**: 청크 크기를 3의 배수인 **8190**으로 변경 → 청크별 base64에 패딩이 없어 이어붙여도 유효
- **교훈**: 청크 단위 `btoa()` 사용 시 청크 크기는 반드시 3의 배수로 할 것

---

### 19. 번역 키 누락 — `t()` 폴백 함정 (2026-07-04) ✅ 해결 완료

- `daq_6510.js`: `t('daq_stop_pending') || '사이클 완료 후 정지...'` 코드에서
  `t()`는 **키가 없으면 키 이름 자체(truthy)를 반환**하므로 `||` 폴백이 절대 실행되지 않음
- 결과: DAQ-6510 사이클 중 정지 시 화면에 원시 키 문자열 `daq_stop_pending`이 그대로 표시
- **해결**: daq_6510.js는 보호 파일이므로 `core.js` T 테이블에 `daq_stop_pending` ko/en 키 추가
  (같은 방식으로 `btn_excel` 키도 추가 — PT-2000 Excel 버튼)
- **교훈**: `t(key) || fallback` 패턴은 동작하지 않음 — 번역 키는 반드시 T 테이블에 등록할 것

---

## ✅ 성공해서 잘 완성된 부분

### 1. SP-2100 / TL-2200 UI 전면 재구성

- 단독 원본 HTML과 동일한 레이아웃 완성
- 라벨 기반 파싱: `AVG`/`PEAK` 라벨 있는 라인만 측정으로 인정
- 그룹-개수 번호 시스템 (`1-1`, `1-2`, `2-1` …)
- 샘플명 인라인 편집, localStorage 설정 영속화 (속도/딜레이/평균시간/그룹수/모델)

---

### 2. Keithley 2700 오버레인지 처리

- `9.90e+37` 반환 시 `"Over Flow"` 표시, `valid: false`로 자동기록 상태머신에서 제외

---

### 3. DAQ-6510 10채널 스캔 — 실기 확인 완료

- 수동 릴레이 제어 방식 확정: `ROUT:OPEN:ALL → ROUT:CLOS(@ch,ch+10) → READ?`
- 채널 딜레이 정밀 보정: `_channelStartTime` 기록 후 경과 시간 차감
- 3분마다 기록 / 실시간 모드 선택
- 동적 X축 스케일 (1000행 미만: auto-fit, 이상: 1시간 단위 ceiling)
- 3컬럼 flex 디스플레이 (슬롯/채널/샘플명 | 저항값 | 상태)

---

### 4. Agilent 4339B 시퀀스 재작성 — 실기 확인 완료

- 정확한 시퀀스: `*RST → *CLS → FUNC 'RES' → TRIG:SOUR BUS → OUTP ON → ABOR → INIT → *TRG → FETC?`
- 차징 카운트다운 디스플레이 표시
- 측정값 `toExponential(4)` (5 유효숫자, 장비 표시와 일치)
- 샘플명 인라인 편집
- 그룹 수 ≥ 2일 때 Box & Whisker 차트 자동 전환

---

### 5. 수염차트 Y축 수동 편집 — 전 계측기 적용 (7종)

- 차트 Y축 위치에 투명 입력창 오버레이, ↺ 초기화 버튼
- 적용: Hioki, Keithley, VL-50, SP-2100, Agilent 4339B, PT-2000, LT-1000

---

### 6. 수염차트 평균값 사선 표기 표준화

- 모든 계측기 수염차트 상단에 단위 포함 평균값 비스듬히 표시 (Ω, mm, g)
- `core.js`(Hioki/Keithley/VL-50)와 각 계측기 파일 모두 통일

---

### 7. LT-1000 팝업 가이드 3종

| 팝업 | 내용 |
|---|---|
| 데이터 전송 방법 | `Select → 🔴Print → Enter → Enter → 자동 기록(녹색)` |
| 로드셀 캘리브레이션 | 5단계 플로우, 1kg 분동 이미지(`assets/LT-1000 1kg.png`) 포함 |
| ST FULL 해결 방법 | `Select → Print → ▲×2 → DL → Enter → 초기화` |

---

### 8. EXE 패키징 파이프라인 표준화

3중 원인 해결 후 안정적인 5단계 빌드 절차 확립:

```powershell
# 1. 읽기전용 해제
attrib -r instruments\*.js

# 2. dist 재생성
python build/build_standalone.py Hioki3540 Keithley2700 MitutoyoVL50 Agilent4339B DAQ6510 SP2100 PT2000 LT1000 AIPhotoEditor ClubExpense

# 3. 폴더명 단축
Rename-Item dist\[긴이름] 3M_Instrument_Logger

# 4. 기존 EXE 종료
Get-Process -Name "3M_Instrument_Logger" -ErrorAction SilentlyContinue | Stop-Process -Force
Remove-Item dist\3M_Instrument_Logger.exe -Force -ErrorAction SilentlyContinue

# 5. 패키징
python build/package_exe.py dist/3M_Instrument_Logger

# 6. 읽기전용 재설정
attrib +r instruments\*.js
```

---

### 9. AI Photo Editor 웹 포팅 완성

- 파일 업로드 + 스마트폰 카메라 연동 (WiFi / USB ADB 터널)
- 휠 줌(0.05~40×), 우클릭 팬, 회전 슬라이더(-15°~15°)
- SAD 기반 크롭 자동 맞춤 (0.2배 축소 그레이스케일 브루트포스)
- ExcelJS 엑셀 내보내기 (크롭 PNG 셀 삽입)
- 덮어쓰기 촬영 (`overwriteIdx`)

---

### 10. AI Photo Editor UI 개선 (2026-06-25)

- 상단 툴바 "한 열" → **"그룹 개수"** 라벨 수정
- 촬영 버튼 위치 변경: 하단 바 우측 → **하단 바 가운데** (대기중 | 촬영 | 다음단계 한 줄 배치)

---

### 11. Mitutoyo VL-50 테스트 방법 연동

- MSL-2(기본), ETM-7 등 드롭다운 선택 → 그룹 개수·컬럼 수 자동 연동
- `hideSplit: true` 적용 (분할 화면 불필요)

---

### 12. manual.html 전체 사용설명서 생성

- 전 계측기(9종) + 공통 기능 + 문제 해결 + 시리얼 설정 빠른 참조표 포함
- 고정 TOC 사이드바, 단일 HTML 파일 (오프라인 사용 가능)
- EXE 배포판에 포함
- 접근: `http://localhost:8000/manual.html`

---

### 13. PST-3202 통합 앱 구현 (2026-07-03)

GW Instek 3채널 DC 전원공급기를 통합 앱에 추가. 단독 프로그램(`PST-3202/index.html`)과 동일한 기능을 `instruments/pst3202.js`로 포팅.

| 기능 | 내용 |
|---|---|
| CH1↔CH2 동기화 토글 | 채널 카드 사이 52px 컬럼 배치, 흘러내리는 점 애니메이션(`pst-flow`), CH1 Vset/Iset 변경 시 CH2 자동 복사 |
| 병렬/직렬 트래킹 안내 | `⚠️` + `title` 툴팁으로 물리 배선 요구 및 독립 측정 불가 안내 |
| OVP 커스텀 모달 | `prompt()` 대신 `pstOvpModal` 커스텀 다이얼로그 |
| 폴링 게이팅 | 출력 OFF 상태에서 폴링 생략 (`!S.outputActive` 조건) |
| CH2 그래프 버그 수정 | "사용 안함" 채널이 그래프에 표시되던 문제 → `isChUsed()` 헬퍼로 해결 |
| 라이트 테마 대응 | 그래프 배경/격자/테두리 테마 분기, LED 디스플레이는 라이트에서도 의도적 다크 유지 |
| 시스템 로그 UI | 둥근 모서리 + 내부 어두운 배경 복원 |

---

### 14. phone.html PWA 설치 (2026-06-25)

| 파일 | 역할 |
|---|---|
| `phone.manifest.json` | 앱 이름·아이콘·배경색 정의 |
| `phone-sw.js` | 서비스워커 (GET 캐시, POST 촬영 요청은 캐시 제외) |
| `pwa-icon-192.png` / `pwa-icon-512.png` | 홈 화면 아이콘 |

- **Android**: Chrome `⋮` → 홈 화면에 추가
- **iPhone**: Safari 공유 → 홈 화면에 추가
- 설치 후 아이콘 탭 하나로 카메라 앱처럼 실행

---

### 15. PST-3202 3채널 동기화 + 입력 실시간 검증 (2026-07-04)

| 기능 | 내용 |
|---|---|
| CH1↔CH2↔CH3 3채널 동기화 | `syncCh3` 토글 — CH3 사양에 맞춰 전압 6V·전류 2A 캡, CH1이 6V 초과 상태에서 켜면 전 채널 6V 자동 클램프 |
| `_effMaxV(ch)` 헬퍼 | 동기화 상태에 따른 유효 최대 전압 중앙 관리 — 사이클 편집기 라벨/검증에도 반영 |
| `validateFixedField()` | V/I 입력 `oninput` 실시간 검증 — 최대값 초과 시 즉시 자동 조정 + 빨간 안내 메시지 |
| setV/setI auto-clamp | alert 팝업 제거 → 자동 클램프 + 인라인 메시지 방식 |
| 전류 표시 표준화 | 모든 채널 전류 입력값 `toFixed(3)` (예: 2 → 2.000) |
| 단독 프로그램 동기화 | `PST-3202/index.html`에도 검증/auto-clamp 동일 반영 |

---

### 16. 전체 코드 점검 + EXE 재빌드 (2026-07-04)

- **문법 검사**: 전 JS 14개 파일 ES 모듈 모드 통과 (중복 선언 없음)
- **레지스트리·번역 키·core.js 클래스 무결성** 전수 확인
- **헤드리스 브라우저 스모크 테스트**: 런처 카드 10개 전부 렌더링 확인
- **dist 동기화**: 스탤 파일 5개 + 누락 assets 3개(Independent/Parallel/Series.png) 복사,
  standalone_entry.js에서 ClubExpense 제거 (웹 앱과 일치)
- **EXE 재빌드**: `dist/3M_Instrument_Logger.exe` (PST-3202 포함 10개 계측기)
- **계측기별 MD 매뉴얼 신규 작성**: `docs/manuals/` — 10개 장비 + 색인,
  PST-3202는 초보자용 상세 매뉴얼(기초 개념·FAQ 포함)

---

### 17. 언어 전환(한국어↔English) 전면 점검 — 전 계측기 EN 렌더 감사 (2026-07-05)

> PST-3202 i18n 작업(성과 #15) 완료 후, "다른 계측기도 언어 전환이 제대로 되는지" 확인차
> 헤드리스 브라우저로 전 계측기를 영어 모드로 렌더링해 잔여 한국어 문자열을 전수 조사.

**조사 방법**: `_i18n_test.html?instr=<이름>&lang=en` 형태의 임시 테스트 페이지를 만들어
`window.app.setLang('en')` → `launchInstrument(...)` 순으로 실행한 뒤 `--dump-dom`으로
렌더링 결과를 저장, 정규식으로 잔여 한글 문자열을 스캔. HTML 주석도 `dump-dom`에 그대로
직렬화되어 나타나므로("--" 접미사로 식별) 실제 화면에 보이는 버그와 구분해서 판별.

**발견·수정한 실제 버그 (모두 core.js 또는 index.html 공통 코드에 있었음 — 계측기별 파일은
Photo Editor 제외 대부분 이미 `t()` 적용돼 있었음)**:

| # | 위치 | 증상 | 원인 | 수정 |
|---|---|---|---|---|
| 1 | `core.js` BoxPlot | Y축 오토스케일 입력창 placeholder/버튼 title이 언어 전환 후에도 "자동"으로 고정 | `inp.placeholder = '자동'` 하드코딩 | `t('y_axis_auto')` + `data-i18n-placeholder`/`data-i18n-title` 속성 추가 |
| 2 | `core.js` `_buildPanels` | 스플릿 그래프 패널의 "측정 데이터가 없습니다" 빈 상태 문구가 하드코딩 | `chart_empty` 키를 안 쓰고 리터럴 삽입 | `${this.t('chart_empty')}`로 교체 |
| 3 | `index.html`/`standalone.html` | 계측기 관리 모달 "예: ETM-9" placeholder가 언어 전환 안 됨 | `placeholder` 속성은 `_applyLang()`이 다루지 않음 | `_applyLang()`에 `data-i18n-placeholder` 지원 추가, `test_name_ph` 키 신설 |
| 4 | `core.js` `_openCardEdit` | 카드 편집 모달의 위치/자산번호/담당자/비고 placeholder 4개가 하드코딩 | 동일 (placeholder 속성) | `card_meta_*_ph` 키 4개 신설, `this.t(...)`로 교체 |
| 5 | **그리드 계측기(Hioki·Keithley·VL-50) — 가장 큰 발견** | 계측기 화면이 열린 채로 언어를 전환하면 중앙 상태표시줄("연결 안됨"), 우측 그래프 패널 제목/힌트/빈상태, 데이터 그리드 헤더가 전혀 재번역 안 됨 | `_rebuildInstrumentSidebar()`의 grid(non-custom) 분기가 사이드바 `dynamicSettings`만 재생성하고 center/rightpanel은 건드리지 않음 — 이 두 영역은 `launchInstrument()` 최초 1회만 빌드됨 | grid 분기에 `_setDisplay(...)` 재호출(미연결 시)과 `_savePanelData()` + `_buildPanels(...)` 재호출 추가 — `setSplit()`이 쓰는 것과 동일한 안전한 재빌드 경로라 기록 데이터 유실 없음 |
| 6 | `sp2100_logger.js`/`agilent_4339b.js`/`pt2000_probe_tack.js`/`lt1000_loop_tack.js`의 `_attachYOverlay` | 위 #1과 동일한 Y축 "자동"/"Y축 자동" 하드코딩이 4개 계측기 파일에 각각 복붙되어 있었음 (core.js BoxPlot과 별개 구현) | 동일 헬퍼 함수가 4곳에 중복 구현됨 | 4개 파일 모두 `t('y_axis_auto')`로 교체 (이미 각 파일에 `t()` 헬퍼 존재 — 재사용) |

**Photo Editor — 별도 대규모 작업** (읽기 전용 보호 파일, 사용자 명시 승인 후 진행):
- 번역 시스템(`t()`)이 **전혀 적용되어 있지 않아** 언어를 바꿔도 아무 것도 안 바뀌는 상태였음
  (기존 `core.js`의 `ai_*` 키 14개는 이전 UI 버전의 죽은 코드 — 현재 화면 어디에서도 참조 안 됨)
- `t()`/`tf()` 헬퍼 신설, 하드코딩 문자열 57건을 전부 `t()`/`tf()` 호출로 교체
  (USB 현미경 UI, 크롭 설정, 파일 목록, 회전, 촬영, 폰 연동, Excel/ZIP 내보내기 상태 메시지 전체)
- `core.js`에 `ai_*` 키 40개 신규 추가, 기존 3개(`ai_per_row`/`ai_export_excel`/`ai_file_count`)는
  실제 UI 문구와 맞지 않아 값 갱신 (예: `ai_per_row` "한 열 개수" → 현재 라벨 "그룹 개수")

**검증**: 10개 계측기 전부 헤드리스 EN 렌더 재검사 완료 — 잔여 한국어 0건
(HTML 주석·"한국어"(언어명 자체, 의도된 표시)·타 모달의 "예:" placeholder 제외).

---

### 18. PST-3202 평가 목록 파일 내보내기/가져오기 — 컴퓨터 간 데이터 이동 (2026-07-05)

> 사용자 요청: "측정완료후 저장하면 평가리스트 저장되는데... 다른컴퓨터에서 해당 파일만 옮겨서
> 불러오면 이전 평가 목록 불러와서 볼수 있게. 나중에 이걸로 엑셀파일로 내보내기도 가능해야해"

**조사 결과**: 측정 완료 시 `PST3202_eval_data.json`(서버/EXE 옆)에 이미 자동 저장되고 있었으나,
그 파일을 다른 컴퓨터로 옮겨 앱에 불러오는 UI가 없었음 — 정확히 같은 상대경로에 수동으로
파일을 놔둬야만 자동 로드되는 상태.

**요구사항 확정을 위한 3가지 질문 후 진행**:
1. 가져오기 방식 → **파일 선택 버튼** (USB 등으로 파일 하나만 들고 다녀도 됨)
2. 병합 방식 → **기존 목록에 추가** (교체 아님), 단 **같은 시간대 동일 평가는 중복 제거**
3. 이동 단위 → **JSON 파일 한 개만** (폴더/EXE 전체 이동 아님)

**구현 (`pst3202.js`)**:
- `exportEvalListFile()`: `S.evalRecords`를 `PST3202_records_<타임스탬프>.json`으로 브라우저
  다운로드 (순수 클라이언트 사이드, blob + `<a download>`)
- `handleImportFile()`: `FileReader`로 읽어 JSON 배열 검증 → **`timestamp` 필드 정확히 일치하면
  중복으로 스킵**, 아니면 로컬 `id` 재할당 후 `S.evalRecords`에 push → 병합 완료 후
  자동으로 `saveEvalRecords()` 호출(이 컴퓨터에도 영구 반영) → 추가/중복 개수를 안내 모달로 표시
- Data Table 패널에 `📤 목록 내보내기` / `📂 목록 가져오기` 버튼 신설
- 기존 `📥 Raw Data (xlsx)` 내보내기가 `S.evalRecords`를 그대로 사용하므로, 가져온 레코드도
  체크 후 바로 엑셀 내보내기 가능 — 별도 구현 없이 기존 경로 재사용

**검증**: 헤드리스 브라우저에서 `app.instr.handleImportFile(...)`을 실제 코드 경로로 호출해
① 신규 2건 정상 추가 ② 동일 데이터 재가져오기 시 0건 추가(중복 제거 확인)
③ 잘못된 파일 형식 시 번역된 경고 정상 표시 — 3가지 시나리오 모두 통과.

---

### 19. PST-3202 매뉴얼 갱신 + PDF 인쇄 렌더링 버그 발견·수정 (2026-07-08)

> 사용자가 CH1↔CH2 사이 접지 단자 용도를 질문 → 답변 내용을 매뉴얼에 반영하는 김에
> "PDF 정리된 내용 줄 바뀜 부자스러운 부분 자연스럽게 바꿔줘, 참고 이미지도 추가해줘" 요청.

**PDF에서 발견한 실제 렌더링 버그 (헤드리스 Edge → PyMuPDF로 PNG 렌더링해 육안 확인)**:
- **번호목록(`<ol>`)의 1. 2. 3. 숫자가 통째로 사라짐**: 인쇄용 CSS의 `* { margin:0; padding:0; }`
  전역 리셋이 `<ol>`의 `padding-left`까지 0으로 만들어 `list-style-position:outside` 마커가
  표시될 공간을 잃음. 글머리 기호(•)는 폭이 좁아도 그럭저럭 남았지만 숫자+마침표는 완전히
  안 보여, "연결 순서"·"5분 요약"·"사이클 모드" 단계 목록이 전부 번호 없이 문단처럼 뚝뚝
  끊겨 표시되고 있었음 — 사용자가 말한 "줄 바뀜 부자스러운" 부분의 정체.
  **수정**: `ul, ol { padding-left: 24px; margin-left: 0; }`
- **`\n`이 원화기호(₩n)로 오식**: 한글 폰트에서 백슬래시 글리프가 ₩ 기호로 렌더링되는
  Windows 한글 코드페이지 특성 — "종단문자: LF (\n)" 표기가 "LF (₩n)"으로 나타남.
  **수정**: 리터럴 백슬래시 대신 "LF (개행문자)"로 문구 자체를 변경.

**이미지 추가 관련 판단**: 앱에 이미 있는 트래킹 모드 참고 이미지(`Independent/Parallel/Series.png`)를
열어봤더니 AI 생성 이미지라 버튼 라벨이 죄다 가짜/오타("PARK/NDEP", "DELAT", "GUTPUT2" 등)라
배선을 잘못 안내할 위험이 있어 **매뉴얼에는 사용하지 않기로 판단**. 대신 표지에는 검증 리스크가
낮은 제품 사진(`PST-3202.png`)만 900px/JPEG 78%로 축소해 삽입, 접지 단자·트래킹 설명은
텍스트/표로만 구성.

**콘텐츠 추가**: "2-1. 접지(GND) 단자는 언제 쓰나요?" 섹션 신설(표+팁 박스), "10-1. 평가 목록을
다른 컴퓨터로 옮기기" 섹션을 PDF에도 반영(기존엔 MD에만 있고 PDF는 그 기능 추가 이전 버전이라
누락돼 있었음), FAQ 2건 추가. 문서 버전 v1.0→v1.1, 7페이지→9페이지.

> ⚠️ **교훈**: 인쇄용 HTML에서 `* {margin:0;padding:0;}` 같은 전역 리셋을 쓸 때는 `ul/ol`에
> `padding-left`를 반드시 별도 지정할 것. 브라우저 미리보기와 실제 `--print-to-pdf` 렌더링
> 결과가 다를 수 있으므로, 완성 후 PyMuPDF로 페이지를 PNG 렌더링해 육안 검증하는 절차가 필요.

---

### 20. 직렬 트래킹 — 그래프·엑셀을 CH1+CH2 합산 단일 값으로 표시 (2026-07-08)

> 사용자 질문: "직렬 트래킹 측정일경우 CH1/CH2 값이 나뉘어 보이는데, 합쳐진 전압/전류가
> 보여지는 게 맞지 않아?"

**전기적 사실관계 확인**: 직렬(Series)은 전압이 합산(V1+V2)되고 전류는 한 회로를 도는
동일한 값(I1=I2, 더하는 게 아님) — 이 계산 로직은 이미 Excel 내보내기 쪽 `evalRecordBlocks`에
구현돼 있었으나, **실시간 그래프에는 전혀 반영이 안 돼 있었고, Excel도 "채널별"(개별) 시트를
항상 만들고 "합산" 시트는 직렬 레코드가 있을 때만 추가로 얹는 구조**라 개별/합산이 혼재돼
있었음. 확인 질문 2개로 요구사항 확정: ① 표시 위치는 그래프+엑셀(사용자가 직접 지정) ②
"개별값은 의미 없다, 직렬 트래킹시에만 합산값만" — 즉 대체가 아니라 완전 교체.

**구현**:
- **그래프**: `_seriesCombinedSource()` 헬퍼로 CH1/CH2 raw 배열을 합산해 **가짜 단일 채널**
  형태로 만들어 `graphSource()`가 반환 — `drawGraph()`의 기존 채널별 순회 로직은 손대지 않고
  `chIdxs:[0]` 하나만 넘기는 것만으로 자동으로 합산 라인 하나 + Y축 자동 스케일(60V 등)이 됨.
  라이브/과거기록 보기 양쪽 다 처리.
- **범례**: 직렬 모드일 때 CH1/CH2/CH3 범례를 전부 숨기고 "직렬 합산 V/I" 항목만 표시.
- **엑셀**: 레코드당 블록을 하나만 반환하도록 리팩터링 — 직렬이면 합산 컬럼, 그 외는 기존
  채널별 컬럼. 기존 2-시트(채널별+합산) 구조를 **시트 1개**로 통합.

**검증**: 실제 코드 경로로 직렬 레코드(CH1=CH2=30V/1A)를 주입 → xlsx 생성 → 다시 파싱해서
셀 값이 정확히 `60.000V / 1.000A` 단일 컬럼으로 나오는지, 독립 레코드는 기존 방식을 유지하는지
직접 확인. 그래프 범례 토글도 `setTM(2)`/`setTM(0)` 실제 호출로 검증.

---

### 21. 🔴 EXE에서 평가 기록이 저장 후 사라지는 버그 — 근본 원인 발견·수정 (2026-07-08)

> 사용자 신고: "테스트하고 저장한 다음, 프로그램 재시작했을때 이전 평가 데이터가 DATA TABLE에
> 있어야 하는데 없어."

**원인 진단 과정**: `saveEvalRecords()`는 성공 여부와 무관하게 `manualSaveRecords()`가 항상
"저장 완료" 모달을 띄우는 구조라, 저장이 실패해도 사용자는 알 방법이 없었음. `server.py`(브라우저
개발 모드)와 `build/package_exe.py`(EXE 내장 서버)를 나란히 비교(`grep evaldata`)해보니 —
**`server.py`에는 `/api/evaldata` GET/POST가 정상 구현돼 있는데, `build/package_exe.py`가
EXE에 심는 내장 HTTP 서버에는 이 라우트가 통째로 없었음**. `/api/storage/*`(설정값 저장용),
`/api/visa/*`, `/api/save-file`만 있고 PST-3202 평가 기록 API는 애초에 이식된 적이 없던 것.
→ EXE에서 저장을 누르면 항상 404 → `catch`에서 에러를 삼켜 "저장 완료"로 보이지만 실제로는
디스크에 아무 것도 안 쓰였고, 재시작 시 `loadEvalRecords()`도 404 → 조용히 빈 상태 유지.

**수정**: `package_exe.py`의 launcher 템플릿(f-string으로 EXE에 내장될 Python 서버 코드를
생성하는 부분)에 `server.py`와 동일한 `/api/evaldata`(GET/POST)·`/api/export`(POST) 라우트를
이식. 저장 경로는 `os.path.dirname(sys.executable)` — EXE 파일이 실제로 위치한 폴더 기준.

**검증 방법이 까다로웠던 점**: PyInstaller로 EXE를 통째로 빌드해서 실행/재시작하는 것은
자동화된 헤드리스 검증이 어려움. 대신 — launcher 템플릿을 실제 문자열로 렌더링(f-string 치환)
→ `webview` import와 창 실행 부분만 잘라내고 `Handler` 클래스 정의까지만 남긴 코드를 별도
포트(18765)에서 순수 `HTTPServer`로 백그라운드 실행 → **curl로 GET(빈 배열 확인)→POST(레코드
저장)→GET(방금 저장한 레코드가 그대로 반환)을 실제로 실행해 "재시작 후에도 남아있다"는
시나리오 자체를 재현**. `PST3202_eval_data.json` 파일이 격리된 테스트 폴더에 실제로 생성되는
것도 파일시스템에서 직접 확인. `/api/export`도 동일 방식으로 파일 생성 확인.

> ⚠️ **교훈**: `server.py`(개발 모드)와 `build/package_exe.py`(EXE 내장 서버)는 완전히 별도로
> 관리되는 두 HTTP 서버 구현체다. 새 `/api/*` 엔드포인트를 하나에 추가하면 **반드시 다른
> 쪽에도 이식했는지 확인**할 것 — 안 그러면 "브라우저에선 되는데 EXE에서만 조용히 실패"하는
> 버그가 재발한다. 이번 건은 애초에 PST-3202 기능(2026-07-03) 추가 당시부터 이식이 누락돼
> 있었던 것으로 보이며, 브라우저(`server.py`)로만 테스트하고 EXE 저장/재시작 시나리오는
> 실제로 검증한 적이 없어서 그동안 발견되지 않았던 것으로 추정.

---

### 22. 🔴 엑셀 내보내기 그래프가 빈 화면으로 나오는 버그 — chIdxs 지역변수 미갱신 (2026-07-08)

> 사용자 신고: "엑셀로 내보내기 했는데 그래프가 안에 그래프가 제대로 표시안돼" (스크린샷 첨부 —
> 축·격자선은 정상인데 데이터 곡선이 전혀 없는 빈 차트)

**배경**: `drawOffscreenGraph()`는 다른 세션이 최근 추가한 새 코드로, PST-3202 평가 기록을
ExcelJS로 내보낼 때 각 레코드의 힘-시간(전압/전류) 곡선을 오프스크린 `<canvas>`에 그려 PNG로
변환해 엑셀 "Graph" 시트에 삽입하는 기능. 기존에 알던 `exportSelectedRawData()`(xlsx 라이브러리
기반)와는 완전히 다른 ExcelJS 기반 구현으로 통째로 재작성돼 있었음 — 단 데이터 계산 로직인
`evalRecordBlocks()`(직렬 합산 포함)는 그대로 재사용하고 있어 그쪽은 문제없었음.

**원인**: 다음 코드에서 `else` 분기(독립/병렬 모드)가 `src.chIdxs`만 채우고 바깥 스코프의
`chIdxs` 변수는 갱신하지 않음:
```js
let chIdxs = [];
if (rec.trackMode === 2) {
  src = _seriesCombinedSource(ts, v, i);
  chIdxs = src.chIdxs;                              // 직렬만 갱신
} else {
  const activeChs = activeChannelsOfRecord(rec);
  src = { ts, v, i, chIdxs: activeChs.map(c => c-1) }; // src.chIdxs만 설정
}
```
독립/병렬 레코드는 `chIdxs`가 항상 빈 배열로 남아 라인 그리기 루프가 0번 실행됨. 게다가 Y축
자동스케일도 같은 `chIdxs`를 써서 계산하다 보니 실제 데이터 대신 fallback 기본값(0.5V,
0.05A)이 쓰였는데, 공교롭게 사용자의 저전압 측정 데이터와 범위가 비슷해서 "축은 그럴듯한데
곡선만 없는" 형태로 나타나 더 헷갈렸음.

**진단**: `CanvasRenderingContext2D.prototype`의 `moveTo`/`lineTo`/`stroke`/`beginPath`를
헤드리스 브라우저에서 몽키패치해 실제 그리기 호출 횟수를 계측 → 격자선(6회)만 호출되고 데이터
라인 그리기는 0회임을 확인해 `chIdxs`가 비어있다고 특정. 함수를 임시로 export에 노출시켜
`drawOffscreenGraph()`를 직접 호출 → 반환된 dataURL을 `<img>`로 렌더링 → 헤드리스 스크린샷으로
버그 재현(사용자 스크린샷과 동일한 빈 차트) → 수정 → 동일 방법으로 재검증(선이 정상적으로
그려짐) 순으로 진행.

**수정**: `chIdxs`를 미리 빈 배열로 선언하지 않고, if/else로 `src`를 먼저 확정한 뒤
`const chIdxs = src.chIdxs;`로 한 번만 파생하도록 리팩터링.

**부수 발견**: 같은 점검 과정에서 `setOvpDirect()`/`setV()`의 OVP 관련 경고 메시지 2건이
i18n 미적용(하드코딩 한글)인 것도 함께 발견해 수정.

---

### 23. 🟢 엑셀 내보내기 — Graph 시트 레이아웃 통일 + V/I 범례 추가 (2026-07-08)

> 사용자 요청: "RAW 데이터시트는 우측으로 이어서 추가되는데.. 그래프시트도 동일하게 상단엔
> 제목 아래는 그래프 이런식으로 우측으로 차례대로 그래프 출력되게해줘" → 이어서
> "그래프는 서로 겹쳐지지않게 출력해주고 전압 전류 잘 표시되게 해줘"

**배경**: #22에서 고친 `drawOffscreenGraph()`가 삽입되는 "Graph" 시트는 레코드마다 제목→이미지→
여백을 아래로 쌓는 세로 레이아웃이었음. 반면 "채널별"(Sheet1)은 레코드마다 좌→우로 이어붙는
가로 레이아웃 — 여러 레코드를 한 번에 내보내면 두 시트의 탐색 방향이 서로 달라 불편했음.

**수정 1 — 레이아웃 통일**: Sheet1의 `let colOffset=0; blocks.forEach(b=>{ b.startCol=colOffset;
colOffset += b.headers.length+GAP; })` 패턴을 Graph 시트에도 적용. 제목은 항상 1행, 그래프
이미지는 항상 2행에 고정하고, 레코드 인덱스마다 컬럼 오프셋(`startCol = idx * colsPerBlock`)만
우측으로 이동시키는 방식으로 재작성. 블록 폭(`colsPerBlock`)은 이미지 폭(600px)을 담는 데
필요한 컬럼 수(`imgCols`)에 여백 컬럼(2컬럼)을 더해 계산 — 픽셀→컬럼 변환 시 실제 Excel 공식보다
일부러 보수적인 값을 써서 어떤 경우에도 이미지끼리 겹치지 않도록 안전 마진을 둠.

**수정 2 — V/I 범례 추가**: 기존엔 차트 좌상단/우상단에 `V`/`A` 한 글자만 표시돼 있어 채널이
여러 개거나 직렬 합산 모드일 때 어떤 선이 무엇을 뜻하는지 알기 어려웠음. 실시간 화면 그래프와
동일한 채널별 색상 팔레트(`GV`/`GI`)를 그대로 재사용해 캔버스 상단에 범례(색상 스와치 + 라벨,
예: `CH1 V`/`CH1 I`, 직렬 트래킹 시 `직렬 합산 V`/`직렬 합산 I`)를 직접 그려 넣음. 화면 그래프와
엑셀 그래프의 색상이 100% 일치해 사용자가 양쪽을 비교하기 쉬워짐.

**검증**: 헤드리스 Edge에서 `drawOffscreenGraph()`를 독립 호출하는 테스트 페이지로 (1) 2채널
독립, (2) 직렬 합산, (3) 3채널 독립 3가지 케이스를 렌더링해 스크린샷 육안 확인 — 범례 색상과
실제 라인 색상이 정확히 일치하고 겹침 없이 읽기 좋게 표시됨을 확인. 컬럼 오프셋 계산은 ExcelJS
CDN 의존성 때문에 헤드리스로 직접 돌리지 않고 픽셀 수학으로 별도 검증(이미지 630px 폭 확보,
다음 블록까지 126px 갭 — 겹침 불가능).

---

## 🗂 관련 파일

| 파일 | 역할 |
|---|---|
| `CLAUDE.md` | 전체 아키텍처·규칙·이슈 공식 인수인계 문서 (SSOT) |
| `docs/INSTRUMENT_COMMUNICATION_PROTOCOLS.md` | 계측기별 통신 설정 검증 문서 |
| `docs/SP2100_PROTOCOL_REFERENCE.md` | SP-2100/TL-2200 파싱 규칙 SSOT |
| `manual.html` | 전체 사용설명서 (브라우저에서 열기) |
| `docs/manuals/` | 계측기별 MD 매뉴얼 (PST-3202 초보자용 포함) |
| `build/build_standalone.py` | 계측기 선택 → dist 폴더 생성 |
| `build/package_exe.py` | dist 폴더 → EXE 패키징 |
| `phone.manifest.json` | PWA 매니페스트 |
| `phone-sw.js` | PWA 서비스워커 |
