# PST-3202 통합 컨트롤러 수정 이력 (3M Instrument Logger)

> 작성일: 2026-07-03
> 수정 파일: instruments/pst3202.js

---

## 1. 비활성화 상태 구분 명확화
- 입력 불가 상태의 채널 카드 및 입력 필드에 비활성화 스타일을 명확히 적용하여 활성/비활성 구분을 시각적으로 개선.

## 2. 단계 추가 글자 크기 및 전압 색상 수정
- 사이클 편집기 폼 글자 크기를 오리지널과 동일하게 조정.
- 전압 설정값 색상을 주황/빨간 계열로 변경.

## 3. 트래킹 모드 창 스타일 개선
- 독립 프로그램 원본 기준으로 트래킹 결선 이미지 팝업 크기, 여백을 동일하게 조정.
- 좌측 사이드바 스크롤바 추가.

## 4. 라이트 테마 설정 정상 반영

## 5. 홈 이동 팝업창 불투명 처리
- 확인 팝업창을 완전 불투명한 단독 팝업으로 변경.

## 6. 장비 연결 시 자동 에러 초기화
- onConnect() 시 *CLS 및 :OUTPut:PROTection:CLEar 자동 전송.

## 7. 프로그램 시작 시 설정값 자동 계측기 입력
- 연결 완료 후 각 채널 전압/전류 현재값을 UI에 자동 표시.

## 8. 허용치 초과 경고 팝업 — 중앙 단독 팝업으로 변경
- alert() 대신 화면 가운데 전용 경고 모달(pstWarningModal) 표시.
- 허용치 범위가 팝업 내용에 명시됨.

## 9. 병렬/직렬 트래킹 채널 전압/전류 실시간 미러링

### 병렬 트래킹 (tm=1)
- CH2 입력 필드 자동 잠금.
- CH1 입력 시 CH2에 실시간 자동 미러링.

### 직렬 트래킹 (tm=2)
- CH1, CH3 입력 필드 자동 잠금.
- CH2 입력 시 CH1에 실시간 자동 미러링.

## 10. 독립 모드 채널 2, 3 사용 토글 기본 꺼짐(OFF)
- 독립 모드 진입 또는 초기 로딩 시 CH2/CH3 사용 스위치 자동 OFF.
- 사용자가 수동으로 켜서 활성화 가능.

## 11. 직렬 트래킹 — OCP/OVP CH2→CH1 자동 미러링
- CH2 OCP 토글 변경 시 CH1 OCP 동기화 및 잠금.
- CH2 OVP 설정 시 CH1에도 동일값 자동 전송.

## 12. 병렬 트래킹 — OCP/OVP CH1→CH2 자동 미러링
- CH1 OCP 토글 변경 시 CH2 OCP 동기화 및 잠금.
- CH1 OVP 설정 시 CH2에도 동일값 자동 전송.

## 13. 실시간 그래프 활성 채널 감지 로직 개선
- 독립 모드에서도 사용(ON) 토글이 켜진 모든 채널이 그래프에 표시.
- syncTrackingSettings() 내에서 updateGraphLegend() 동시 호출.

## 14. OCP/OVP 버튼 항상 클릭 가능하도록 변경
- 장치 미연결 시에도 OCP/OVP 설정 클릭 가능.
- 실제 하드웨어 명령은 연결된 경우에만 전송.

## 15. 장치 연결 시 즉시 폴링 시작
- 출력(OUTPUT) 상태와 무관하게 연결 즉시 doPoll() 시작.
- 연결 즉시 실시간 전압/전류 측정값이 화면에 표시됨.

## 16. 데이터 테이블 영구 저장 구조
- 저장 버튼 → PST3202_eval_data.json 로컬 기록.
- 프로그램 재시작 시 이전 이력 자동 복원.

## 17. 트래킹 결선 이미지 팝업 크기 축소
- max-width: 90vw → 60vw / max-height: 90vh → 60vh (약 33% 축소)

## 18. 저장 완료 / 엑셀 내보내기 — 전용 모달 팝업 표시
- alert() 대신 화면 가운데 전용 정보 모달(pstInfoModal) 표시.
- 저장 완료: 💾 저장 완료
- 엑셀 내보내기: 📊 엑셀 내보내기 완료

## 19. 사이클 시퀀스 편집기 헤더 OCP/OVP 제어 장치 추가
- 사이클 편집기 폼 우측 상단(CH 탭 바로 옆)에 현재 편집 중인 채널의 OCP 보호 토글 스위치 및 OVP 설정 버튼 배치.
- 메인 채널 카드의 OCP/OVP 설정값 및 비활성화 상태(트래킹 모드 등으로 인한 잠금)와 실시간 양방향 동기화.

---

## 수정 파일 목록

| 파일 | 변경 내용 |
|------|----------|
| instruments/pst3202.js | 위 모든 수정 사항 및 OCP/OVP 헤더 제어 기능 반영 |
| server.py | /api/evaldata GET/POST 엔드포인트 (영구 저장) |

---

## 20. OVP 직관적 입력 폼 변경 및 자동 동기화 고도화 (2026-07-08)

### OVP 버튼 제거 및 인라인 입력 칸 구성
- 채널 카드 및 사이클 편집기 헤더의 **[OVP 설정] 버튼을 완전히 제거하고 숫자 입력 필드로 대체**.
- 마우스 클릭 실수를 방지하기 위해 **모든 숫자 입력 필드의 증감 화살표(스피너)를 CSS로 숨김**.

### OVP 기본값 및 스펙 검증
- 프로그램 시작 시 OVP 기본값을 `null` 대신 **각 채널 최대 물리 스펙 전압(CH1/2: 32.00V, CH3: 6.00V)**으로 자동 할당.
- 32.00V를 초과하는 전압 또는 OVP 값을 입력 시 최대 전압 한계값(32.00V)으로 자동 보정 및 스펙 초과 경고 표시.

### 실시간 상호 검증 로직 개편 (동일 전압 허용)
- OVP 값과 전압 설정값이 정확히 같을 때(예: 전압 32V / OVP 32V)는 **경고를 발생시키지 않고 정상 작동**하도록 조건 완화.
- 설정 전압이 OVP 설정값보다 클(초과) 경우에만 OVP 값으로 자동 제한하여 차단을 방지하는 실시간 경고 및 교정 기능 적용.
- OVP 값을 설정 전압 미만으로 낮추어 적을 시, 자동으로 설정 전압과 같게 OVP를 상향 조정하여 기기 차단 오작동 예방.

### 실시간 입력 동기화 및 탭 전환 유지 오류 해결
- **실시간 input 감지**: 인라인 OVP 필드에 실시간 키보드 입력(`input` 이벤트) 시 내부 프로그램 상태 데이터(`S.ch[ch-1].ovpVal`)에 즉각 반영하여 동기화.
- **탭 유지**: OVP 값 입력 후 사이클 탭(`CH1` ➔ `CH2`)을 이동했다가 돌아와도 **입력한 OVP 값이 초기화되지 않고 유지**되도록 조치.
- **동기화 즉시 미러링**: `CH1` OVP 변경 시 `CH2`(및 3채널) OVP 값이 실시간으로 미러링되며, **[동기화] 스위치를 켜는 즉시** 1채널에 적혀 있던 OVP 값이 2, 3채널 입력란에 즉각 채워지도록 수정.
- **사이클 및 모드 동기화**: [동기화] 스위치를 켤 때, 1채널의 모드(고정/사이클)와 설정한 모든 사이클 단계(`S.cySteps`)가 2채널에 복제되고 하단 편집기에 `CH2` 탭이 즉시 자동 활성화되도록 연동.
- **구문 오류(SyntaxError) 해결**: 미러링 괄호 누락으로 인한 런타임 중단 해결.
